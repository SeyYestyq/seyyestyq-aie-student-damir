
# Чек-лист самопроверки проекта — Drop Forecaster

> Проект: **Drop Forecaster — прогнозирование снижения цены на государственных тендерах**
> Автор: **Ахмеров Дамир Зинурович**

---

## Оценка по десятибалльному чек-листу

| № | Критерий | Выполнено | Комментарий |
|---|----------|-----------|-------------|
| 1 | **Паспорт проекта** (README.md) | да| Заполнен: название, автор, описание, ссылка на репозиторий, контакты |
| 2 | **EDA и аудит данных** (ноутбук) | да | `notebooks/01_eda_and_data_audit.ipynb` — пропуски, дисбалансы, визуализация, статистика |
| 3 | **Эксперименты с моделями** (ноутбук + артефакты) | да | `notebooks/02_model_comparison.ipynb` — CatBoost vs XGBoost vs RandomForest vs LightGBM vs Ridge |
| 4 | **Выбор финальной модели с обоснованием** | да | CatBoost выбран по метрикам MAE, RMSE, R² — задокументировано в отчёте и ноутбуке |
| 5 | **Оформленный сервис (API)** | да | `src/api.py` — FastAPI с `/predict`, `/predict-batch`, `/health`, `/info` |
| 6 | **Unit-тесты** | да | `tests/test_model.py` — тесты модели, API-endpoint, схемы Pydantic |
| 7 | **Конфигурация через переменные среды** | да | `src/config.py` + `configs/.env.example` — Pydantic Settings + .env |
| 8 | **Контейнеризация (Docker)** | да | `Dockerfile` + `docker-compose.yml` в корневом каталоге проекта |
| 9 | **Наблюдаемость (логирование, health-check)** | да | Loguru + `/health` endpoint + structured JSON logs |
| 10 | **Отчёт (report.md)** | да | Полный отчёт: предметная область, EDA, эксперименты, архитектура, результаты, выводы |
да
---

## Дополнительные бонусы (не входят в обязательный чеклист)

| Бонус | Выполнено | Комментарий |
|-------|-----------|-------------|
| Нейросетевой базлайн (MLP) | ✅ | MLPRegressor(128→64→32) — сравнение с CatBoost |
| MLflow трекинг экспериментов | ✅ | `mlruns/` — параметры, метрики, теги для 6 моделей |
| Скрипт обучения `src/train.py` | ✅ | End-to-end: данные → обучение → MLflow → сохранение модели |
| Пакетная обработка (`/predict-batch`) | ✅ | Поддержка до 100 объектов в одном запросе |
| Feature importance интерпретация | ✅ | CatBoost built-in feature importance |
| Документация API (Swagger) | ✅ | Авто-генерация через FastAPI `/docs` |
| Демо-данные для тестирования | ✅ | `data/history_drop_dataset.csv` (5000 строк) |

---

## Итоговая самооценка

**Баллы:** 10 / 10

**Оценка по шкале курса:**
- 10 баллов → оценка **5 (отлично)**

---

## Сценарий демонстрации на защите

1. Показать структуру проекта и README
2. Открыть EDA-ноутбук — показать визуализации и выводы
3. Открыть ноутбук сравнения моделей — показать таблицу метрик
4. Запустить API: `uvicorn src.api:app --reload`
5. Отправить тестовый запрос через Swagger UI (`/docs`)
6. Показать health-check и логи
7. Показать unit-тесты: `pytest tests/ -v`
8. Обсудить выбор модели и дальнейшие шаги
